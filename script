alias ls='ls --color=auto'

FCEDIT=vim
PS1='\[$(tput setaf $colour)\] \W \[$(tput sgr0)\]'

win() { cmd.exe /C "$@"; }
acp() { ghcs "add, write an appropriate commit message for, then push: `git diff --cached`"; }
tname() { printf "\033]0;$*\007"; }
